---
interface Props {
  id: string;
  title: string;
}

const { id, title } = Astro.props;
---

<section id={id} class="mb-20">
  <div class="text-2xl font-extrabold font-serif uppercase mb-14">
    {title}
  </div>

  <slot />
</section>
